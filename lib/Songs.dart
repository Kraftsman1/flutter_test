import 'package:flutter/material.dart';

class Songs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Songs List Goes Here'),
      ),
    );
  }
}